name: carbon_elt
version: 0.1.0
config-version: 2

profile: carbon_elt

model-paths: [models]
test-paths: [tests/dbt]
seed-paths: [seeds]
macro-paths: [macros]

target-path: target
clean-targets:
  - target
  - dbt_packages
